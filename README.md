# Лабораторная работа №1 — Вёрстка веб‑калькулятора (HTML/CSS)

## Содержание

- [Задание](#задание)
- [Цель](#цель)
- [Вариант и референсы (Интерньука)](#вариант-и-референсы-интерньука)
- [Реализация](#реализация)
  - [Структура страницы (HTML)](#структура-страницы-html)
  - [Стили (CSS)](#стили-css)
- [Доп. вопросы на защите (реализованные фичи)](#доп-вопросы-на-защите-реализованные-фичи)
  - [1) Как реализована сетка кнопок с помощью CSS Grid Layout и как заставить кнопки занимать несколько колонок?](#1-как-реализована-сетка-кнопок-с-помощью-css-grid-layout-и-как-заставить-кнопки-занимать-несколько-колонок)
  - [2) Как реализовать различие кнопок по функционалу одним базовым классом?](#2-как-реализовать-различие-кнопок-по-функционалу-одним-базовым-классом)
  - [3) Как оформить интерактивные состояния кнопки (:hover) с плавной анимацией?](#3-как-оформить-интерактивные-состояния-кнопки-hover-с-плавной-анимацией)

---

## Задание

1. Сверстать интерфейс простого калькулятора в HTML.
2. Подключить таблицу стилей и оформить калькулятор с помощью CSS.
3. Применить классы для:
   - кнопок цифр;
   - кнопок второстепенных операций;
   - кнопок основных операций;
   - поля вывода результата.
4. Дополнительно (в рамках варианта/самостоятельных пунктов):
   - центрировать калькулятор на странице;
   - сделать кнопки квадратными со скруглением;
   - добавить интерактивные состояния при наведении/нажатии.
5. Продемонстрировать страницу, используя веб-сервер или расширение для IDE.

## Цель

Познакомиться с основами построения пользовательских интерфейсов web‑страниц средствами **HTML** и **CSS**: разметка, стилизация элементов, работа с классами/селекторами, базовые интерактивные состояния (`:hover`) и разметка компонентов по сетке (CSS Grid).

## Вариант и референсы (Интерньука)

**Вариант:** стилизация интерфейса под инновационное издательство «ИНТЕРНЫУКА» (светло-зеленая цветовая гамма, серые и белые плашки, тени, скругления) + использование стандартных и кастомных шрифтов (Segoe UI).

**Референсы:**

- Сайт издательства «Интерньука» (оригинал): https://www.internauka.org/

## Реализация

### Структура страницы (HTML)

- Основной контейнер калькулятора: `.calculator-wrapper`.
- Поле вывода результата (дисплей): `#calc-display` с атрибутом `readonly`.
- Блок кнопок: `.calc-buttons` (CSS Grid).
- Базовый класс для кнопок: `.calc-btn` с дополнительными классами-модификаторами.

Фрагмент разметки из `calc.html`:

```html
<div class="calculator-wrapper">
    <input type="text" id="calc-display" readonly placeholder="0">

    <div class="calc-buttons">
        <button class="calc-btn clear">C</button>
        <button class="calc-btn operator">÷</button>
        <button class="calc-btn operator">×</button>

        <button class="calc-btn">7</button>
        <button class="calc-btn">8</button>
        <button class="calc-btn">9</button>
        <button class="calc-btn operator">-</button>
        <!-- ... -->
        <button class="calc-btn">0</button>
        <button class="calc-btn">.</button>
        <button class="calc-btn equal">=</button>
    </div>
</div>
```

### Стили (CSS)

В `calc.css` реализованы:
- размещение сетки кнопок калькулятора с помощью CSS Grid;
- стили контейнера калькулятора (фон `#4c5053`, тень, скругления);
- стили для дисплея калькулятора (выравнивание текста по правому краю, шрифт `monospace`, скругление `8px`);
- базовый стиль кнопки + модификаторы `.operator`, `.clear`, `.equal`;
- анимация/обратная связь на `:hover` (увеличение яркости и легкое смещение вверх).

Ключевые фрагменты из `calc.css`:

```css
.calculator-wrapper {
    max-width: 350px;
    margin: 40px auto;
    background: #4c5053;
    padding: 25px;
    border-radius: 15px;
    box-shadow: 0 15px 30px rgba(0,0,0,0.3);
}

#calc-display {
    width: 100%;
    height: 60px;
    font-size: 28px;
    text-align: right;
    margin-bottom: 20px;
    padding: 15px;
    box-sizing: border-box;
    border: none;
    background: #f4f4f4;
    border-radius: 8px;
    color: #333;
    font-family: monospace;
}
```

---

## Доп. вопросы на защите (реализованные фичи)

### 1) Как реализована сетка кнопок с помощью CSS Grid Layout и как заставить кнопки занимать несколько колонок?

Grid-контейнер `.calc-buttons` делит ширину на 4 равные колонки с помощью `grid-template-columns`. Чтобы кнопка занимала несколько колонок (например, кнопка сброса `C` или равенства `=`), к ней применяется свойство `grid-column: span 2`.

```css
.calc-buttons {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 12px;
}

.calc-btn.clear { 
    background: #d9534f; 
    grid-column: span 2; 
}
```

### 2) Как реализовать различие кнопок по функционалу одним базовым классом?

Используется подход: **базовый класс** `.calc-btn` + **классы-модификаторы** (`.operator`, `.clear`, `.equal`). Базовый класс определяет общие размеры, отступы, скругления, курсор и переходы, а модификаторы изменяют фоновый цвет под назначение кнопки.

```css
.calc-btn {
    padding: 20px;
    font-size: 20px;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    background: #8cc63f;
    color: white;
    transition: all 0.2s;
}

.calc-btn.operator { 
    background: #666; 
}

.calc-btn.equal { 
    background: #5cb85c; 
    grid-column: span 2; 
}
```

### 3) Как оформить интерактивные состояния кнопки (:hover) с плавной анимацией?

Состояние наведения оформлено через псевдокласс `:hover`. Мы увеличиваем яркость кнопки и приподнимаем её вверх с помощью `transform: translateY`. Плавность перехода обеспечивается свойством `transition` на базовом классе.

```css
.calc-btn {
    /* ... */
    transition: all 0.2s;
}

.calc-btn:hover {
    filter: brightness(1.1);
    transform: translateY(-2px);
}
```
